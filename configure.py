import os, sys
import sipconfig

diana_src_dir = sys.argv[1]

config = sipconfig.Configuration()

# The name of the SIP build file generated by SIP and used by the build
# system.
sip_files_dir = "sip"
sip_files = ["diana.sip", "metlibs.sip"]

# Run SIP to generate the code.
output_dirs = []

for sip_file in sip_files:

    output_dir = sip_file.split(".")[0]
    build_file = output_dir + ".sbf"
    
    if not os.path.exists(output_dir):
        os.mkdir(output_dir)
    
    command = " ".join(
        [config.sip_bin, "-c", output_dir, "-b", os.path.join(output_dir, build_file),
         "-I"+config.sip_inc_dir, "-I"+diana_src_dir, "-I/usr/include/metlibs", os.path.join(sip_files_dir, sip_file)]
        )
    print command
    os.system(command)
    
    # Create the Makefile (within the diana directory).
    makefile = sipconfig.SIPModuleMakefile(
        config, build_file, dir=output_dir
        )
    makefile.extra_include_dirs.append(diana_src_dir)
    makefile.extra_include_dirs.append("/usr/include/metlibs")
    makefile.extra_lib_dirs.append(diana_src_dir)
    makefile.extra_libs.append("diana")
    
    makefile.generate()
    
    output_dirs.append(output_dir)

# Generate the top-level Makefile.
sipconfig.ParentMakefile(
    configuration = config,
    subdirs = output_dirs
    ).generate()
